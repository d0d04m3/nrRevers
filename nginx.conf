worker_processes 1;
events { worker_connections 1024; }
http {
  upstream backend {
        ip_hash;
        server node-red1:1880;
        server node-red2:1880;
        

        
    }

   server {
        listen 80;
        location / {
        proxy_pass http://backend;
       
        }
    }
}
